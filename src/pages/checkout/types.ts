export type ClientType = {
  firstName: string;
  lastName: string;
  address: string;
  city: string;
  state: string;
  phoneNumber: string;
  zipCode: string;
};

export type ClientFormType = ClientType;
